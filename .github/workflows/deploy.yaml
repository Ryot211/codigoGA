name: Deploy to server

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    env:
      NAME: CodigoFacilito

    steps:
      - name: Hola Mundo
        run: ls - al

      
      - name: Action
        user: actions/checkout@v4

      - name: Working dir after action
        uses: ls -al
  
  second_job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_job
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola mundo dos ${{github.ref}}"
        

